{
  "buildCommand": "echo 'Vercel handles React builds automatically'",
  "outputDirectory": "dist",
  "framework": "create-react-app",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}